import Navlinks from "./Navlinks";
import User from "./User";

const Navbar = () => {
  return (
    <div className="navbar">
      <Navlinks />
      <User />
    </div>
  );
};

export default Navbar;
